# 📋 Правило отображения кнопок оплаты для заказов

## 🎯 Задача

Реализовать правильное отображение кнопок оплаты в зависимости от типа оплаты заказа.

## 📐 Правило

### Условие
Если `is_cash_payment == True` И `payment_status == "NOT_PAID"`, то:
- ❌ **НЕ показывать** кнопку "🔍 Проверь оплату"
- ✅ **Показывать** кнопку "💰 Принять оплату"

### Логика работы

#### Для наличных заказов (`is_cash_payment=True`):
1. **Кнопка "💰 Принять оплату"** появляется вместо "🔍 Проверь оплату"
2. При нажатии кнопки:
   - Бот просит отфотографировать купюры
   - Курьер отправляет фото купюр (можно несколько фото)
   - Фото сохраняются в массиве `pay_photo` заказа
   - После отправки фото показывается кнопка "✅ Завершить заказ"
   - При завершении заказ получает `payment_status="PAID"`

#### Для не наличных заказов (`is_cash_payment=False`):
1. **Кнопка "🔍 Проверь оплату"** показывается для заказов с `NOT_PAID`
2. При нажатии кнопки:
   - Бот проверяет статус оплаты в Odoo
   - Если оплата подтверждена в Odoo → `payment_status` обновляется на `PAID`
   - Если оплата не подтверждена → отправляется сообщение менеджеру в Odoo
   - После проверки кнопка меняется на "✅ Заказ выполнен"

## 🔍 Обоснование

### Почему для наличных не показывается "Проверь оплату"?

**Причина:** Менеджер не может поставить статус "оплачено" для наличных заказов в Odoo до встречи курьера с клиентом, так как:
- Оплата происходит физически при передаче денег
- Менеджер не знает, получил ли курьер деньги
- Проверка в Odoo для наличных заказов всегда вернет `NOT_PAID`

### Почему для наличных показывается "Принять оплату"?

**Причина:** Курьер должен сам подтвердить получение денег:
- Курьер физически получает деньги от клиента
- Курьер фотографирует купюры как подтверждение
- Только после этого заказ может быть завершен с `PAID`

## 📊 Матрица отображения кнопок

| payment_status | is_cash_payment | Показываемая кнопка |
|----------------|-----------------|---------------------|
| `PAID` | любое | ✅ Заказ выполнен |
| `NOT_PAID` | `True` | 💰 Принять оплату |
| `NOT_PAID` | `False` | 🔍 Проверь оплату |

## ⚠️ Важные моменты

1. **Кнопки никогда не показываются одновременно:**
   - Либо "💰 Принять оплату" (для наличных)
   - Либо "🔍 Проверь оплату" (для не наличных)
   - Либо "✅ Заказ выполнен" (если оплата подтверждена)

2. **Защитная проверка:**
   - В функции `cb_order_check_payment` есть проверка на наличные заказы
   - Если курьер каким-то образом попытается проверить оплату наличного заказа, показывается сообщение с инструкцией

3. **Тестовые заказы:**
   - Для тестовых заказов (отрицательный `external_id`) логика работает так же
   - При проверке оплаты для тестовых заказов автоматически устанавливается `PAID`

## 📝 Реализация

### Файлы с реализацией:
- `keyboards/orders_kb.py` - функция `in_transit_kb()` - логика отображения кнопок
- `handlers/orders.py` - функция `cb_order_accept_payment()` - обработка принятия оплаты
- `handlers/orders.py` - функция `cb_order_check_payment()` - защитная проверка для наличных
- `handlers/photo.py` - обработка фото купюр

### Комментарии в коде:
Все функции содержат подробные комментарии с описанием правил и логики работы.

